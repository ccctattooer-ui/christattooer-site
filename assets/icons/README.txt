Icon artwork for the site chrome. Two sizes of each:
  <name>-32.png    for the 16px menubar strip on the catalog pages
  <name>-128.png   for the 56px ParlorOS desktop icons
Thumbnails in between are generated at build time, so only these two are needed.
See the icon work order for the full list, sizes and palette.
